rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /talentProfiles/{uid}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == uid && request.resource.size < 100 * 1024 * 1024;
    }

    match /recruiterProfiles/{uid}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == uid && request.resource.size < 10 * 1024 * 1024;
    }

    match /verificationDocs/{uid}/{allPaths=**} {
      allow read: if request.auth.uid == uid || request.auth.token.admin == true;
      allow write: if request.auth.uid == uid && request.resource.size < 10 * 1024 * 1024;
    }
  }
}
